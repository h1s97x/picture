# 图片命名规范（NORMALIZING）

本文件定义本项目图片文件的**命名规范**。上传新图片前请先阅读本规范；命名不规范的图片将无法通过 CI 检查。

> 适用范围：`images/` 下**新上传**的图片（即 `images/` 顶层平铺的新图）。
> `images/_legacy/` 为历史归档（856 张，只读不动），不在此规范约束范围内。

## 1. 命名规则总览

```
<语义名>-<日期YYYYMMDD>.<扩展名>
```

一个文件名 = **语义名 + 上传日期 + 扩展名**，各部分用 `-` 连接。

示例：

| ✅ 合规 | ❌ 违规 | 原因 |
|---|---|---|
| `mushoku-tensei-20260302.webp` | `20260302-Mushoku-Tensei.webp` | 日期应在**末尾**；文件名不允许大写 |
| `yuki-20260302.jpg` | `yuki.jpg` | 缺少日期 |
| `roxy-20260302.webp` | `roxy.webp` | 缺少日期 |
| `pixy-banner-20260806.png` | `图片-20260806.png` | 不允许中文 |
| `screenshot-20260806.png` | `Screenshot-20260806.png` | 不允许大写 |
| `game-capture-20260806.png` | `游戏截图-20260806.png` | 不允许中文 |

## 2. 详细规则

| 规则 | 说明 | 示例 |
|---|---|---|
| **2.1 字符集** | 只允许 `a-z`、`0-9`、`-`、`_`（全小写，无空格） | `yuki-no-yume-20260302` ✅ |
| **2.2 语义名** | 用 `-` 连接的英文单词，描述图片内容，避免纯日期或纯乱码 | `mushoku-tensei`、`game-screenshot` |
| **2.3 日期段** | 固定 `YYYYMMDD`（8 位，上传当天），放在文件名**末尾** | `-20260302` |
| **2.4 扩展名** | 小写，保留原始格式 | `.png` `.jpg` `.webp` `.gif` |
| **2.5 分隔符** | 统一用 `-`；不允许连续分隔符（`--`/`__`） | `a-b` ✅，`a--b` ❌ |
| **2.6 首尾字符** | 不允许以 `-` 或 `_` 开头/结尾 | `-x.webp` ❌ |
| **2.7 保留目录** | `_legacy/` 为历史归档，禁止写入新图 | — |

## 3. 禁止的命名（乱码来源）

以下均为历史中出现的乱码命名，**新上传图片一律禁止**：

- ❌ 中文 / URL 编码（`%E5%8D%A1...` / `图片.png` / `拼图2.png`）
- ❌ 大写字母 / 空格 / 平台后缀（`20260302-Mushoku-Tensei.webp` / `a.png!small` / `x.png@1256w_120h`）
- ❌ 哈希值（`0004154d4fac44d7a0bc2ba839d2133c1652345857.png`）
- ❌ 纯数字 / 纯日期（`01.png` / `20160315114913.png`）
- ❌ 无扩展名 / 非图片扩展名（`100x100` / `20151110221036496`）

## 4. 日期段的作用

日期 `YYYYMMDD` 带来两个好处：

1. **天然防重名**：同一张图重复上传（即使语义名相同），日期不同也不会覆盖；
2. **便于治理**：按时间排序即可看到上传进度，方便清理旧图。

> 同一主题一天内上传多张图时，可在日期前追加序号或内容细分，例如 `banner-home-20260806.png`、`banner-about-20260806.png`。

## 5. 上传前自检

在仓库根目录执行：

```bash
# 自检指定图片
scripts/validate-images.sh <文件1> <文件2>

# 自检仓库内所有已提交图片（不含 _legacy）
scripts/validate-images.sh
```

也可用 `--strict` 开启最严模式（要求必须有语义名 + 日期段），用于最终治理验收：

```bash
scripts/validate-images.sh --strict
```

## 6. 常见问题

**Q1：忘记传日期怎么办？**
CI 会拦截并给出告警。本地可用当天日期快速重命名：

```bash
mv <旧名>.png <语义名>-$(date +%Y%m%d).png
```

**Q2：图片内容很杂，不好起语义名？**
按来源/用途归类即可，例如 `screenshot`、`meme`、`photo`、`icon`、`banner`，配合日期即合规。

**Q3：历史乱码图需要处理吗？**
不需要。历史图已归档到 `images/_legacy/`（只读），规范只约束后续新上传的图片。

## 7. 变更记录

| 日期 | 说明 |
|---|---|
| 2026-08-06 | 制定命名规范，落地 README 与 CI 检查 |
